.claude/
node_modules/
.env
.env.local
# .env 를 압축해 옮기다 커밋에 딸려 들어간 적이 있다(키가 들어 있었다).
# 이름을 조금만 바꿔도(envb.zip) 빠져나가므로 env* 압축은 통째로 막는다.
env*.zip
*.env.zip
dist/
.next/

# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/

# Local dev databases
*.db
*.sqlite3

# 로컬 작업 문서 (github 업로드 제외)
docs/

# 배포 산출물 (deploy/build.ps1) — deploy/dist/ 는 위 dist/ 규칙으로 이미 무시됨.

# Render
.render/

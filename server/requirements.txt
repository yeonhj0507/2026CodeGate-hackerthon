# --- 공통 (담당2 인증 + 담당3 도메인이 하나의 앱을 공유) ---
fastapi>=0.110
uvicorn[standard]>=0.29
SQLAlchemy[asyncio]>=2.0
asyncpg>=0.29
alembic>=1.13
pydantic[email]>=2.6
pydantic-settings>=2.2

# --- 담당2: 인증/계정 ---
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1            # passlib 1.7.4 호환 (4.1+ 에서 __about__ 경고/에러 방지)
PyJWT>=2.8
slowapi>=0.1.9

# --- 담당3: 도메인 ---
pgvector>=0.3            # 추천 유사도 검색(스트레치) + partner_articles.embedding 컬럼
anthropic>=0.42          # 퀴즈 생성·개념 재요약 (LLM_PROVIDER=claude 일 때만 호출)
httpx>=0.27              # 데모 스크립트 · 테스트 클라이언트

# --- dev/test ---
aiosqlite>=0.19          # 로컬 스모크 테스트용(Postgres 없이 빠른 확인). 운영은 Postgres.
pytest>=8.0
pytest-asyncio>=0.23
